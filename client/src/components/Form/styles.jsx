import { makeStyles, Theme } from '@material-ui/core/styles';

export default makeStyles(theme => ({
  buttonCaption: {
    alignSelf: 'flex-end',
    marginBottom: '0.3em',
    marginRight: '1em',
  },
}));
